# Docker Configuration
COMPOSE_PROJECT_NAME=aiStocks

# PHP App Configuration
# Port to access the application (http://localhost:APP_PORT)
APP_PORT=8080

# Note: The project structure is:
# /aiStocks (root)
# ├── index.php, reportManager.php, generateReports.php (public files)
# ├── /app (application code)
# ├── /data (stock data and templates)
# ├── /reports (generated reports - auto-created)
# ├── /images (uploaded images - auto-created)
# └── reportSettings.json (settings storage)
